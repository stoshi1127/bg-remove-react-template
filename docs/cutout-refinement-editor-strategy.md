# 仕上げ修正エディタ 方針・進行状況

最終更新: 2026-09-17  
現在の状態: **Phase 1 完了 / Phase 2（MVP実装）進行中**  
実装状況: **通常透過・高精度透過向けの手動修正MVPを実装済み。自動テスト・本番ビルド確認済み。実機確認とPro制限は未完了**

## 1. この文書の目的

背景透過後の画像を部分的に修正できる「仕上げ修正エディタ」について、プロダクト方針、MVPの範囲、技術上の前提、Pro提供方法、検証指標、実装状況を一か所で管理する。

作業を進めるたびに、この文書の「進行状況」とチェックリストを更新する。コードが存在するだけでは完了扱いにせず、テストと計測確認まで終わった段階で各Phaseを完了とする。

## 2. 決定した方針

- アンケート結果を待たず、仕上げ修正エディタのMVP実装へ進む。
- 最初はAI処理ではなく、ブラウザ内で動作する手動の「消去ブラシ」「復元ブラシ」を実装する。
- 背景透過の結果を部分的に直せない現状を解消し、画像を完成させられることを最優先にする。
- AI消しゴム、自動輪郭補正、不要物の生成塗りつぶしは、手動エディタの利用状況を確認してから追加する。
- アンケートによる希望ではなく、エディタの起動、ブラシ使用、修正適用、保存、Pro導線到達という実利用で需要を検証する。
- 既存の背景透過、背景合成、ダウンロードの流れを壊さず、結果画像ごとの追加操作として導入する。

## 3. 現在提供している関連機能

### Free・ゲスト

- 標準の背景透過
- 最大30枚の一括処理
- JPG、PNG、WebP、HEIC/HEIFの読み込み
- 背景なし、単色、既存背景テンプレート
- `1:1`、`16:9`、`4:3`、元画像、被写体フィット
- 個別保存、ZIP一括保存
- 4MBを超える画像の自動圧縮
- 別ページのイージートリミング、イージートーン

### Pro

- 高精度背景透過
- 最大25MBを目安とする元画像処理
- 任意の背景画像アップロード
- AI背景生成
- ナチュラルブレンド
- 1K、2K、4K高画質化と一括高画質化
- プレミアムAI月30回
- 広告非表示

### 現在不足している機能

- 背景の消し残しだけを部分的に消す操作
- 誤って消えた髪、服、商品などを部分的に戻す操作
- ブラシで修正した透過状態を、選択済みの背景へ再合成する操作
- 修正の取り消し・やり直し
- AI消しゴム（公開画面では「準備中」）

## 4. なぜ先に実装するのか

- 背景透過サービスの中心作業そのものを完成させる機能で、特定用途に限定されない。
- 個人SNS、仕事資料、EC、デザインのいずれでも、輪郭や消し残しの修正は発生し得る。
- 現在の「標準」または「高精度で全体を再処理」だけでは、一部分だけを直せない。
- 手動編集基盤は、将来のAI消しゴムや自動輪郭補正にも再利用できる。
- ブラウザ内Canvasで実装できれば、手動修正自体の追加AI原価を抑えられる。

## 5. MVPの対象範囲

### 必須

- 結果画像ごとの「切り抜きを修正」ボタン
- 消去ブラシ：被写体として残っている不要部分のアルファ値を下げる
- 復元ブラシ：処理に使用した元画像から画素を戻し、アルファ値を上げる
- ブラシサイズ変更
- ズーム、パン
- 元に戻す、やり直す
- 修正前後の比較
- 適用、キャンセル
- 修正した透過画像を、現在選択中の背景・比率へ再合成
- 修正後の個別保存とZIP保存
- マウス、タッチ、ペンをPointer Eventsで扱う
- キーボード操作、フォーカス管理、ボタン名などの基本的なアクセシビリティ

### MVPで対応する処理

- 標準背景透過
- Pro高精度背景透過
- 背景なし
- 単色背景
- 既存テンプレート背景
- Proの任意背景画像（通常合成のみ）

### MVPでは対応しない処理

- AI背景生成後の完成画像
- ナチュラルブレンド後の完成画像
- AIによる不要物削除・生成塗りつぶし
- 自動輪郭検出・自動エッジ補正
- レイヤー編集
- テキスト、図形、フィルター編集
- 複数画像を同時に同じブラシ操作で編集
- 編集プロジェクトのサーバー保存

AI背景生成とナチュラルブレンドは、背景と被写体が統合された完成画像を返す。通常の透過マスク編集とはデータ構造が異なるため、MVPでは「切り抜きを修正」を表示しないか、非対応理由を明示する。

## 6. 推奨する利用制限

この項目は**最終決定前**である。

推奨案:

- Free・ゲスト：最初の1枚を修正して保存できる
- Pro：手動の消去・復元ブラシを枚数制限なく利用できる
- 将来のAI消しゴム：プレミアムAI回数を消費する

無料体験の条件は編集開始前に表示する。編集を完了した後で突然保存をブロックする方式は採用しない。

未決事項:

- 1枚無料の単位をブラウザ、日、月のどれにするか
- 未ログイン利用者の無料体験済み状態をどこへ保存するか
- Pro契約が無効になった場合の編集画面の扱い
- 手動修正を全利用者へ無料開放し、AI補助だけをProにする案との比較

## 7. 現行実装から見た技術上の前提

現在の通常処理は、おおむね次の順序になっている。

1. 元画像をHEIC変換またはFree向け圧縮する。
2. `/api/remove-bg`から透過画像を取得する。
3. 透過画像から被写体のバウンディングボックスを計算する。
4. 必要に応じて比率変更・背景合成を行う。
5. 合成後の`finalUrl`を`InFile.outputUrl`として保持する。

現状は背景合成前の透過画像URLを恒常的に保持していない。復元ブラシには、透過画像と同じ座標・寸法の元画像が必要になる。

### 想定するデータ追加

名称は詳細設計時に確定する。

- `processedSourceUrl`：実際に背景除去へ渡した元画像。HEIC変換・Free圧縮後の状態を保持する。
- `transparentOutputUrl`：背景合成前の透過画像。
- `refinedTransparentOutputUrl`：ブラシ修正後の透過画像。
- `hasRefinement`：修正適用済みかどうか。

### 想定する処理フロー

1. 背景除去へ渡す画像を、復元用ソースとして保持する。
2. `/api/remove-bg`の透過結果を`transparentOutputUrl`として保持する。
3. 通常どおり背景・比率を適用して`outputUrl`を作る。
4. エディタは`processedSourceUrl`と`transparentOutputUrl`を同じCanvas座標で重ねる。
5. 消去ブラシはマスクを透明へ、復元ブラシは元画像を不透明へ変更する。
6. 適用時に透過PNGを生成し、選択中の背景・比率を再適用する。
7. `outputUrl`、バウンディングボックス、保存対象URLを更新する。

### 詳細設計で確認する項目

- Free圧縮後の画像と透過結果の寸法が常に一致するか
- `fit-subject`を再計算するタイミング
- 修正後のバウンディングボックス再計算
- Undo/Redoを画像スナップショットで持つか、マスク差分で持つか
- 高解像度画像に対するCanvasメモリ上限
- Blob URL、Data URL、Vercel Blob URLのCORSとCanvas汚染対策
- 画面を閉じたときのObject URL解放
- 修正後に高画質化した場合のURL更新順序

## 8. UI方針

### 結果画面

- 各結果画像の「保存」「イージートリミングで編集」と同じ操作領域へ「切り抜きを修正」を追加する。
- AI背景生成・ナチュラルブレンド結果では、MVP中はボタンを非表示にする。
- 無料体験を導入する場合は、ボタン付近に「1枚無料で試せます」を事前表示する。

### エディタ

- PCでは中央に大きなCanvas、周囲にツールを配置する。
- スマートフォンではCanvasを優先し、主要ツールを画面下部へ固定する。
- 背景は市松模様を基本とし、白・黒背景への切り替えも検討する。
- 消去と復元を色だけで区別せず、アイコンと文字を併用する。
- 適用前に修正結果を確認できる状態を保つ。

## 9. 計測方針

画像、ファイル名、ブラシ座標、マスクデータはGA4へ送信しない。

候補イベント:

| イベント | タイミング |
| --- | --- |
| `refinement_editor_view` | エディタを開いた |
| `refinement_editor_started` | 最初のブラシ操作を行った |
| `refinement_editor_applied` | 修正を適用した |
| `refinement_editor_canceled` | 修正を適用せず閉じた |
| `refinement_export_completed` | 修正後画像を保存した |
| `refinement_trial_used` | Free体験を消費した |
| `refinement_pro_click` | エディタ関連のPro導線を押した |

候補パラメータ:

- `user_plan`：`guest` / `free` / `pro`
- `processing_mode`：`standard` / `pro_high_precision`
- `tool_used`：`erase` / `restore` / `both`
- `placement`：`result_card` / `editor` / `export`
- `is_trial`：無料体験か

1ストロークごとのイベントは送らない。編集セッション単位で一度だけ集計する。

### 確認する指標

- 処理完了ユーザーに対するエディタ起動率
- エディタ起動からブラシ使用までの到達率
- ブラシ使用から修正適用までの完了率
- 修正適用後の保存率
- Free体験利用者のPro導線クリック率
- Free体験利用者の決済開始・購入完了率
- 消去、復元、両方の利用構成
- 再訪時のエディタ再利用率

## 10. テスト方針

### 機能

- 消去ブラシで透明になる
- 復元ブラシで元画像の画素が戻る
- 細いブラシ、大きいブラシが正しく動く
- Undo/Redoの順序が崩れない
- 適用、キャンセル、再編集が正しく動く
- 修正後の背景なし、単色、テンプレート、任意背景の再合成
- `1:1`、`16:9`、`4:3`、元画像、被写体フィットの再出力
- 個別保存、ZIP保存、高画質化との連携

### 入力

- JPG、PNG、WebP、HEIC/HEIF
- Free圧縮された画像
- 透明部分を含む入力画像
- 縦長、横長、正方形
- 大きい画像、複数画像

### 操作環境

- マウス
- タッチ
- ペン入力
- キーボードのみ
- モバイル縦画面
- PCの狭い画面と広い画面

### 非機能

- 大画像でブラウザがメモリ不足にならない
- Canvas汚染やCORSエラーが発生しない
- エディタを閉じた後にObject URLを解放する
- 編集中に画像を外部へ追加送信しない
- 既存の背景透過、AI背景、高画質化、ダウンロードを壊さない

## 11. 進行状況

### Phase 0：方針決定 — 完了

- [x] 現在のFree・Pro機能をコードと公開画面で確認
- [x] 利用用途アンケートの集計を確認
- [x] 既存機能と重複しない課題を整理
- [x] アンケート結果を待たずMVPを実装する方針を決定
- [x] 手動の消去・復元ブラシを先行する方針を決定
- [x] AI背景生成・ナチュラルブレンドをMVP対象外とする方針を整理
- [ ] Free体験とPro制限の最終仕様を決定

### Phase 1：詳細設計 — 機能設計完了（Pro制限・GA4管理設定は保留）

- [x] `InFile`へ保持する元画像・透過画像・修正画像の状態を確定
- [x] 通常処理から背景合成前の透過画像を保持する設計を確定
- [x] エディタのCanvas・マスク構造を確定
- [x] Undo/Redoを正規化座標のブラシストロークで保持する方式を確定
- [x] 表示Canvasを長辺最大1400px、適用時の出力を最大4000万画素とする初期メモリ対策を設定
- [x] 背景・比率の再合成フローを確定
- [ ] Free体験・Pro制限の実装方式を確定
- [x] GA4イベント名・パラメータを確定して実装
- [ ] `tool_used`等のGA4カスタム定義が必要か確認
- [x] UI構成を実装で具体化
- [x] テストケースを具体化

### Phase 2：MVP実装 — 進行中

- [x] 透過画像・処理元画像の保持
- [x] エディタコンポーネント作成
- [x] 消去ブラシ
- [x] 復元ブラシ
- [x] ブラシサイズ変更
- [x] ズーム・パン
- [x] Undo/Redo
- [x] Before/After確認
- [x] 修正適用・キャンセル
- [x] 背景・比率の再合成
- [x] 個別保存・ZIP保存との連携
- [ ] Free体験・Pro制限
- [x] GA4計測
- [x] ダイアログ、ボタン名、キーボードショートカット等の基本アクセシビリティ対応

### Phase 3：検証 — 一部着手

- [x] エディタの基本UI・ツール切替・キャンセル・Escape・ブラシ適用の単体テスト
- [x] 本番ビルドとTypeScript検査
- [x] 既存を含む自動テスト39件の回帰確認
- [x] 修正後画像の保存成功時だけ`refinement_export_completed`を送るよう計測を確認
- [x] 高精度で作り直した画像と復元ブラシの参照元が同じ処理用Blobになるよう統合
- [ ] 結合テスト
- [ ] PCブラウザ確認
- [ ] スマートフォン確認
- [ ] 大画像・HEIC・Free圧縮確認
- [ ] 通常背景、単色、テンプレート、任意背景確認
- [ ] GA4 DebugView確認
- [ ] 既存機能の回帰確認
- [ ] 本番公開
- [ ] 公開後の利用指標確認

### Phase 4：次機能の判断 — 未着手

- [ ] 消去・復元の利用構成を評価
- [ ] AI消しゴム追加の優先度を判断
- [ ] 自動輪郭補正追加の優先度を判断
- [ ] 用途別完成テンプレートとの優先順位を再評価
- [ ] Proプランの説明と料金表を更新

## 12. 次に行う作業

1. 実画像で標準透過・高精度透過・単色・テンプレート背景の修正と再合成を確認する。
2. スマートフォン相当の画面でブラシ、ズーム、スクロールを確認する。
3. Free体験とPro制限の最終方針を決めて実装する。
4. GA4 DebugViewで修正エディタのイベント受信を確認する。
5. 回帰テスト後に本番公開する。

## 13. 現在の実装上の暫定仕様

- Free・ゲスト・Proの全プランで修正ボタンを表示する。Pro制限はまだ適用していない。
- 通常の標準透過とPro高精度透過だけを対象とする。
- AI背景生成・ナチュラルブレンド結果には修正ボタンを表示しない。
- 編集表示用Canvasは長辺最大1400pxへ縮小するが、ストロークは正規化座標で保持し、適用時に元の透過画像サイズへ再生する。
- 適用時のブラウザ負荷を抑えるため、4000万画素を超える画像は現時点では修正を確定できない。
- 修正履歴はエディタを閉じるまでのセッション内だけで保持する。

## 14. 直近の検証記録

2026-09-17:

- Jest: 7スイート、39テスト成功
- Next.js本番ビルド: 成功
- TypeScript・Lint: エラーなし
- 既知の警告: `src/app/layout.tsx`のカスタムフォントに関する既存のNext.js警告1件
- 単体保存の完了イベントを、保存処理成功後に送信するよう修正
- HEIC変換を含む高精度再処理で、復元ブラシがAPIへ渡したものと同じ処理用画像を参照するよう修正
- ローカル開発では、設定を明示しない限りVercel Blob直送を使わず、multipart送信・画像バイナリ応答へ自動フォールバックするよう修正
- Vercel Previewへデプロイし、ビルド成功・公開URLのHTTP 200応答を確認
  - Preview: `https://bg-remove-react-template-704u12t45.vercel.app`
  - Deployment: `dpl_9Y9cr31SVLmYyVddEg48yXjMFqpX`
- PC・スマートフォン共通の編集UIをブラッシュアップ
  - PCの「移動」ツールとスマートフォンの2本指パン・ピンチズーム
  - 最大500%ズームと全体表示
  - Canvas上のブラシ径カーソルとツールバー内のサイズ見本
  - 復元モードで復元可能な元画像を半透明表示
  - 元画像表示・修正前表示を分離
  - Undo・Redoをヘッダーのアイコン操作へ変更
  - スマートフォンを`100dvh`の全画面構成にし、下部操作へセーフエリアを適用
  - 更新版Preview: `https://bg-remove-react-template-q9b1fbbc7.vercel.app`
  - Deployment: `dpl_BwWNsXsZDRdghka8CBdpSak33GGt`
  - PCの非描画時もCanvas上のブラシ円がマウスへ追従するよう修正
  - 不正確になり得るツールバー内の円見本を廃止し、ブラシ直径をpx表示
  - ツール名を「消す／戻す」から「透過／復元」へ変更
  - 最新Preview: `https://bg-remove-react-template-4zp4f4vv2.vercel.app`
  - Deployment: `dpl_XEwez2sFoNTTtCea6g8R9dqMWBK9`
  - 操作案内を端末別にし、PCでは「移動ツールでドラッグ」、スマートフォン側では「2本指で移動/拡大」と表示
  - 「修正前」「元画像」の選択中は、チェック・背景色・枠色で状態を明示
  - 最新Preview: `https://bg-remove-react-template-6pqqlk0ff.vercel.app`
  - Deployment: `dpl_AtbBySCX7yXxKAcNGEKuZfVaGojY`
  - PC・タブレットのモーダル高を`92dvh`（最大900px）へ固定し、Canvasの拡大・縮小でモーダル高が変化しない構造へ修正
  - 最新Preview: `https://bg-remove-react-template-lvicma14c.vercel.app`
  - Deployment: `dpl_CtQY9zMBRnTeMeDE7uPYDV5TMoLd`

## 15. 関連ファイル

- `src/components/BgRemover.tsx`：背景透過、背景合成、結果表示、保存、高画質化
- `src/components/CutoutRefinementEditor.tsx`：消去・復元ブラシ、ズーム、Undo/Redo、修正適用UI
- `src/components/__tests__/CutoutRefinementEditor.test.tsx`：修正エディタの単体テスト
- `src/components/PricingTable.tsx`：Free・Pro比較
- `src/components/PremiumFeatures.tsx`：AI背景合成、準備中のAI消しゴム表示
- `src/components/UsageSurvey.tsx`：現行の利用用途アンケート
- `src/lib/analytics/events.ts`：GA4イベント送信
- `docs/usage-survey.md`：アンケート仕様
- `docs/ga4-event-measurement.md`：既存イベント計測仕様
