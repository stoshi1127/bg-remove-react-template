# タスクリスト（追加要望）

## 1. 「AIで背景を作る」セクションの移動
- [x] 「背景をカスタマイズ（オプション）」セクション内から削除
- [x] 「仕上がりモード」セクションの直下に移動させる
- [x] 関連するStateや処理（プリセットリセット等）の整合性確認

## 2. 「自然になじませる」の表示およびスクロール制御
- [x] `selectedTemplate` がない状態でも、常にチェックボックス自体は表示領域を確保させる（無効状態で表示）
- [x] 背景（テンプレ/カラー/カスタム画像）を選択した際に、チェックボックスがアクティブになり、そこへ自動スクロールする処理を追加
- [x] セクションへのスクロール用の参照 (`sectionBlendRef`) を追加
- [x] JSXの構文エラー（閉じタグの不整合等）の修正
