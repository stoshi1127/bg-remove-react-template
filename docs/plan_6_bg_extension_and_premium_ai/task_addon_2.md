# タスクリスト（追加要望 2）

## 1. 「AIで背景を作る」を「仕上がりモード」の選択肢に変更
- [x] 「標準」「高精度（Pro）」に並ぶ形で「✨ AIで背景を作る」という3つ目の選択肢をUI上に追加する。
- [x] 選択されたモードを管理するState (`selectedProcessingMode`) に `'ai_generate'` 等の新しい値を許容し、選択を反映できるようにする。

## 2. モード選択時のUI出し分け制御
- [x] **AIで背景を作る が選ばれた場合のみ**:
  - スタイル（プリセット）やプロンプトの入力欄を表示する。
  - 既存の「背景をカスタマイズ（オプション）」セクションを非表示にする。
  - 「自然になじませる」チェックボックスを非表示にする。
- [x] **標準 または 高精度（Pro）が選ばれた場合**:
  - スタイルやプロンプト入力欄は非表示にする。
  - 「背景をカスタマイズ（オプション）」および「自然になじませる」を表示する（元の挙動）。

## 3. 実装の整合性確認
- [x] 処理開始時の API 呼び出しなど、モードに基づいた処理分岐が正しく動くか再確認する。
- [x] JSXの構文エラーや不要なタグが発生しないように注意して組み込む。
