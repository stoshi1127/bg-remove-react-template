# タスクリスト（追加要望 3）

## 1. 処理中モーダルの背景レイアウト修正
- [x] モーダルの背景となる半透明のオーバーレイ (`fixed inset-0` 等) が画面全体を覆うようにCSSクラスを確認・修正する。（親要素に `relative` や `overflow-hidden` などの影響を受けていないかチェックする）

## 2. 「AIで背景を作る」ボタンのデザイン調整
- [x] ボタンの色合い（強い紫色）を他のボタン（標準や高精度）と調和するよう、少し落ち着いた色味（あるいはテーマカラー）に変更する。
- [x] 「✨」などの絵文字を外し、シンプルなテキストにする。

## 3. アクションボタンのテキスト変更
- [x] 「AIで背景を作る」モードが選択されている場合、または何らかの背景カスタマイズが選択されている場合、メインのアクションボタンのテキストを「背景を透過する」から「背景を合成する」に変更する。
