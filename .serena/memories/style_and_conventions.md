Codebase uses TypeScript with React function components and Tailwind utility classes inline in JSX. Naming is descriptive camelCase for variables/functions and PascalCase for components/types. App Router pages are under src/app. Styling is mostly done with Tailwind classes plus a small set of CSS variables and utility classes in src/app/globals.css. Japanese copy is embedded directly in components.